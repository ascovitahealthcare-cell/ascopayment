# ── Secrets — NEVER commit these ──
.env
.env.local
.env.production

# ── Node modules — too large for GitHub ──
node_modules/
npm-debug.log*

# ── OS files ──
.DS_Store
Thumbs.db

# ── Editor files ──
.vscode/
.idea/
*.swp
